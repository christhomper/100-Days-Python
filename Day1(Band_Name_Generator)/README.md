# 🎸 Band Name Generator

A fun and simple Python project that generates a band name based on user input. This is a beginner-friendly program that uses basic input/output and string manipulation.

## ▶️ How It Works

1. Prompts the user to enter the city they grew up in.
2. Prompts the user to enter the name of a pet.
3. Combines the two inputs to generate a potential band name.

## 🧠 Concepts Practiced

- Using `input()` to gather user data
- Using `print()` to display output
- Basic string concatenation
- Simple user interaction